import { Helmet } from "react-helmet";

const Menu = () => {
  return (
    <div>
      <Helmet>
        <title>Bistro Boss | Menu</title>
      </Helmet>
      menu
    </div>
  );
};

export default Menu;
